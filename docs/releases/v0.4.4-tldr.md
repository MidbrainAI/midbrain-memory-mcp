# v0.4.4 TLDR

- Browser/device-code installer auth is now the default first-run path.
- `npx midbrain-memory-mcp install` can create/select an agent and write keys.
- `--no-login` correctly stays on the manual key fallback path.
- README onboarding now matches the new FTUX flow.
- Device-auth and installer-boundary tests cover the release.
