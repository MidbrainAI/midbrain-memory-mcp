# v0.4.3 TLDR

- Automatic PK injection is disabled by default.
- Legacy injection only runs with `MIDBRAIN_ENABLE_PK_INJECTION=1`.
- Generated configs and hooks do not set that flag.
- Capture, explicit memory tools, and legacy assistant PK scrub remain.
