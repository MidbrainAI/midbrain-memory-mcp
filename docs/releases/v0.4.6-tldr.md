# v0.4.6 TLDR

- Global installs write one shared key by default.
- Interactive multi-client installs can still choose distinct per-client keys.
- Non-interactive installs never rewrite per-client key files.
- Hooks and plugins now use bounded, leveled logger helpers.
- Debug request detail is suppressed unless explicitly enabled.
