# v0.4.2 TLDR

v0.4.2 fixes Codex memory capture reliability after the v0.4.x Codex launch.

- Codex trusts hooks by command identity.
- Old MidBrain Codex hook commands could change when npm cache paths, package
  paths, or Node/Homebrew paths changed.
- If Codex required `/hooks` re-approval and it was missed, capture could
  silently stop.
- Codex hooks now use:

  ```text
  ~/.midbrain/bin/codex-hook user
  ~/.midbrain/bin/codex-hook tool
  ~/.midbrain/bin/codex-hook assistant
  ```

- Codex users may need to approve MidBrain once with `/hooks` after v0.4.2.
- Normal package, npm cache, and Node updates should stop forcing repeated
  approval after that.
- The trust tradeoff is explicit: approving the shim means trusting MidBrain's
  auto-updating package command through `midbrain-memory-mcp@latest`, not one
  specific npm cache file.
- Also included: legacy hook migration, foreign hook preservation,
  conservative startup repair, Codex hook CLI dispatch, docs updates, and
  regression tests.
- Breaking changes: None.
